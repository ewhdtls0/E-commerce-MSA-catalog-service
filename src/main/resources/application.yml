server:
  port: 0

spring:
  application:
    name: catalog-service
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

eureka:
  instance:
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.instance-id:${random.value}} # ?? ???? hostname:0? ?? ????? hostname:${random.value}?? n?? ???? ???? ????? id ??
  client:
    register-with-eureka: true # ???? ??????
    fetch-registry: true # ?????? ?????? ??? ????? ????
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka # ?? ??? ??????? ?????? ??

logging:
  level:
    my.ecommerce.catalogservice: DEBUG